services:
  video-relay:
    image: ghcr.io/jsxiaojun/new-api-video:latest
    pull_policy: always
    restart: unless-stopped
    env_file: .env
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
